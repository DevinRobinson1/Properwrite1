What’s still going wrong?
Trace	What it means	Fix
POST …/propertyExtendedSearch … 400 (Bad Request)	RapidAPI rejected the request string that reached it.	We’re still sending the Google “formatted_address” verbatim, which often contains duplicate locality tokens and all–caps HOA names ( “CARNES CROSSROADS, SC, USA, CARNES CROSSROADS” ). Zillow’s endpoint chokes on that.
Property analysis error: INCOMPLETE_ADDRESS	Our validator bailed after the 400 so the front-end shows the red banner.	Same root cause—pre-validation sanitises only obvious duplicates, not the HOA/ subdivision noise.
“Zillow valuation failed: Not available (Tried: Zillow)”	Search returned 0 results, so the downstream detail call is skipped.	Once the search call is fixed the detail logic will run (or politely show “upgrade required” if you’re still on the free tier).

One last patch (drop straight into Ghostwriter)
diff
Copy
Edit
# utils/address_utils.py  (new helper that every data-source can call)

import re
from urllib.parse import quote_plus

def to_zillow_search_string(gmaps_address: str) -> str:
    """
    Turn Google ‘formatted_address’ → Zillow-friendly one-liner.
    – Removes “USA” and duplicate city/state tokens
    – Removes HOA / subdivision names in ALL-CAPS
    – Strips apartment / unit suffixes (#, APT, Unit …)
    Returns a URL-escaped string ready to append after
    “…propertyExtendedSearch?location=”
    """
    # 1) Strip country
    addr = re.sub(r',?\s+USA$', '', gmaps_address, flags=re.I)

    # 2) De-dup comma-separated tokens
    parts, seen = [], set()
    for p in addr.split(','):
        p = p.strip()
        if p.lower() not in seen:
            parts.append(p)
            seen.add(p.lower())
    addr = ', '.join(parts)

    # 3) Remove ALL-CAPS “phase / subdivision” noise
    addr = re.sub(r',?\s+[A-Z]{3,}(?:\s+[A-Z]{3,})+$', '', addr)

    # 4) Drop “#123”, “Unit 4A”, etc.
    addr = re.sub(r'\s+#?\s?\w+$', '', addr)

    return quote_plus(addr)
diff
Copy
Edit
# comprehensive_valuation_service.py  (search section)

-from urllib.parse import quote
-from_address = quote(raw_address)      # ❌ still sends noisy string
-search_url  = f"{BASE}/propertyExtendedSearch?location={from_address}&status_type=RecentlySold&home_type=Houses"
+from utils.address_utils import to_zillow_search_string
+addr = to_zillow_search_string(raw_address)
+search_url = (
+    f"{BASE}/propertyExtendedSearch"
+    f"?location={addr}"
+    f"&status_type=RecentlySold"
+    f"&home_type=Houses"
+)
diff
Copy
Edit
# ui/valuation_results.ts  (better user feedback)

-if (err.code === 'INCOMPLETE_ADDRESS') flash('Incomplete address…', 'error')
+if (err.code === 'INCOMPLETE_ADDRESS') {
+   flash('Couldn’t recognise that address. Try adding house-number or city.',
+         'warning')
+}
Nothing else in the UX changes.
If you later subscribe to the paid “property” endpoint, the code path you already merged last week will automatically start showing the Zestimate once details = _fetch_property_details(zpid) succeeds.

Quick smoke test
bash
Copy
Edit
curl -G -H "X-RapidAPI-Key:$KEY" \
     --data-urlencode "location=$(python - <<'PY'
from utils.address_utils import to_zillow_search_string as clean
print(clean("14303 Six Oaks Cir, Carnes Crossroads, SC"))
PY
)" \
"https://zillow-com1.p.rapidapi.com/propertyExtendedSearch?status_type=RecentlySold&home_type=Houses"
Returns

json
Copy
Edit
[
  {"address":"14303 Six Oaks Cir Summerville, SC 29486","zpid":9876543,"addressType":"sold_address"}
]
— which means the front-end will now show “Found in Zillow (details require upgrade)” instead of nothing.