Here’s the prompt you can give directly to Replit or your dev team to handle this properly:

⸻

🛠️ Prompt for Replit — Auth & Team Functionality Audit (Duplicate + Bug Cleanup)

⸻

Priority: Audit and clean up all code related to authentication, session handling, and team member invites.
Note: Do not alter any layout, format, or UI/UX structure. Keep the styling and component structure exactly as-is. This audit is purely about back-end and state logic reliability.

⸻

🚨 Problem Summary
	•	Multiple users are reporting issues with:
	•	Logging in and being redirected incorrectly
	•	Getting logged out unexpectedly
	•	Inconsistent session persistence
	•	Errors or non-responsiveness when inviting team members
	•	Auth flows behaving differently depending on entry point (e.g., from blur-gate vs. from header)

These issues appear to stem from duplicate handlers, overlapping logic, and redundant API calls or listeners scattered across auth and team management code.

⸻

🔍 Required Audit Steps

Area	Tasks
Auth / Sessions	- Review /auth/* routes for duplicated login/logout handlers- Ensure there is a single source of truth for:   • JWT or cookie-based session   • Session expiration   • Post-login redirect logic
User Store / Context	- Check if there are multiple useUser() or context providers conflicting- Consolidate to one global user provider used throughout the app
Team Invites	- Ensure team invite logic is not duplicated between admin panel and onboarding- Confirm invited users are always assigned to a team ID & permission level on acceptance
Stripe / Coinbase Sync	- Validate webhook & user metadata logic isn’t repeated in two different flows- Prevent duplicate customer creation on sign-up
API Endpoints	- Look for redundant endpoints like /api/login, /api/auth/login, etc.- Remove any shadow routes or non-used old handlers


⸻

✅ Deliverables
	•	One canonical login / logout flow (shared across blur-gate, header, register modal, etc.)
	•	No duplicate invite handling logic (whether via link or admin panel)
	•	Team seats deducted reliably when member joins
	•	All user-facing actions emit logs to console or dashboard for traceability
	•	Remove or merge duplicate modules, context providers, and event handlers
	•	Confirm session is persistent across browser reload (unless explicitly signed out)

⸻

Please submit PR fix/auth-team-duplication with:
	•	No layout/UX edits
	•	Fully working login/logout/team invite flows
	•	Removed or merged duplicate handlers
	•	Cleaned context/store logic

Let us know when staging is ready so we can QA it across login, team management, and invite redemption scenarios.