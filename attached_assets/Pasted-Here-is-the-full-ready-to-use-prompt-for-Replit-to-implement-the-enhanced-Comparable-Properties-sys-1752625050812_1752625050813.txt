Here is the full, ready-to-use prompt for Replit to implement the enhanced Comparable Properties system with strict underwriting logic, Zillow API integration, accurate distance calculations, and a premium UI experience:

⸻

Prompt for Replit: Build & Integrate Enhanced Comparable Properties System

⸻

Please implement a complete overhaul of the Comparable Properties (Comps) section without affecting or changing any other sections (e.g., Acquisitions, Dispositions, Construction, JV, etc.). This system should accurately underwrite comparable sales using my Zillow API via RapidAPI and meet the following exact requirements:

⸻

🔧 Core Functionality:
	•	Primary API: Continue using propertyExtendedSearch from the Zillow API (https://rapidapi.com/apimaker/api/zillow-com1), authenticated via my existing PRO-tier subscription.
	•	Search Criteria: Only pull sold properties that are:
	•	The same beds and baths (exact match).
	•	Within ±500 sqft of the subject’s square footage.
	•	Of the same property type (e.g., single-family only).
	•	Not waterfront, not in an HOA, and no pool, unless the subject has those features.
	•	Timeframe Rules:
	•	Start with comps sold in the last 90 days.
	•	If fewer than 3 are found, expand to 180 days, then 365 days max.
	•	Distance Rules:
	•	Search radius starts at 0.25 miles, expands to 0.5 miles, then 1.0 mile max if needed.
	•	DO NOT cross major roads or subdivisions. Prioritize ZIP code and neighborhood matching.
	•	Distance Calculation:
	•	Fix the “999 miles” bug by pulling accurate latitude and longitude from Zillow’s API response for both subject and comp properties.
	•	Use the Haversine formula or similar to compute distance and direction.
	•	Sorting Logic:
	•	Sort comps by proximity first, then recency.
	•	Display the top 3–5 closest and most recent comps.
	•	Fallback Handling:
	•	If fewer than 3 comps are found after exhausting radius/time filters, show a clean “No comparable sales found” message with suggestions.

⸻

🖼️ Front-End UI:
	•	Card Format for Comps:
	•	Property image from Zillow
	•	Sold price (e.g., “$374,000”)
	•	Beds / Baths / SqFt (e.g., “3 bd, 2 ba, 1,900 sqft”)
	•	Distance from subject (e.g., “0.3 mi away”)
	•	Address (e.g., “14328 Evening Flight Ln”)
	•	Carousel-style horizontal scroll for comps
	•	Mini-map or embedded Google Maps if possible, showing subject & comps (subject = green pin, comps = blue pins)
	•	“Work in Progress” banner can remain until refinement is complete

⸻

💡 Business Logic Underwriting Adjustments (if needed later):

These values can be referenced later if adjustment logic is layered in:
	•	Bedroom: ±$10K to $25K
	•	Full Bathroom: ±$10K to $15K
	•	Half Bath: ±$5K to $10K
	•	Garage/Carport/Pool: Respect guidance only if adjustments are required

⸻

✅ Implementation Notes:
	•	Do not affect any other functionality in the app
	•	Ensure all comp filtering, geocoding, and API querying is modular and encapsulated
	•	No duplicate API calls—use caching or memory to avoid excess Zillow usage
	•	Make sure comps work with previously cached properties and leverage any stored coordinates

⸻

🧪 Testing Checklist:
	•	Ensure at least 3 comparable properties load correctly for known working addresses like:
	•	14303 Evening Flight Lane, Charlotte, NC 28262
	•	Verify accurate distances display (e.g., “0.4 mi”)
	•	Comps match same bed/bath, within ±500 sqft
	•	Properties with pools or HOA are excluded when subject doesn’t have them
	•	Distance expands correctly if fewer than 3 comps found
	•	No Zillow rate limit errors or unnecessary API re-hits

⸻

Please let me know once this has been implemented, and keep all existing visual formatting consistent with the rest of the app. Do not adjust unrelated layout or styling without approval.

Let’s roll this out ASAP.

⸻

Let me know if you’d like a version adapted for Jira, Notion, or Slack.