Upgrade JV-Deals Admin Panel + Team Notifications
1 | Admin-Side Enhancements
Feature	Tech Notes
Sortable / Filterable Grid	Replace static <table> with DataGrid component (e.g. MUI DataGrid).
• Enable column sort & multi-filter on:
  • state, city, zip (location)
  • submitted_by (user)
  • status (approved / denied / pending)
• Quick-search box to filter all textual columns.
User “Portfolio” Drawer	Click on a submitted_by cell → open side-drawer showing:
• Name, email, phone
• # total submissions, # approved, # denied
• Paginated list of that user’s deals with deep-link (“View”)
Deal Detail Modal	Replace alert popup with modal showing full payload (address, pricing, docs).
Approve / Deny buttons live-update status and emit Zapier trigger (see § 3).
Metrics Chips	Header stats: “Total JV Deals”, “Pending Review”, “Approval Rate”. Auto-refresh after any mutation.

2 | DB / API Updates
jv_deals Table — add composite indices on (state, city, zip) and (submitted_by).

users Table — ensure phone_number is nullable=false going forward; migrate legacy rows.

Endpoints

GET /admin/jv-deals?state=&city=&submitted_by=&status=&page=&limit=

GET /admin/users/:id/jv-deals (for drawer)

PATCH /admin/jv-deals/:id → { status, notes }

3 | Zapier / Notifications
Trigger	Zapier Action
New JV Deal (status=pending)	Zap “JV Deal – Review Needed” → Gmail / Slack channel
Payload: deal ID, address, partner, asking price, suggested offer, link to admin modal.
Status updated to approved or denied	Zap “JV Deal – Decision Sent” → email partner via SendGrid template (includes next steps).

Use Zapier Webhooks → catch POST https://hooks.zapier.com/hooks/catch/... from backend events.

4 | Team-Side Viewer (No Admin Portal Access)
Build a /team/jv-queue route (read-only) that lists only pending deals with same DataGrid but no mutating actions.

Auth via “Team” role; hide for normal users.

5 | QA & Acceptance
 Sort / filter works on > 1 k rows in < 300 ms.

 Drawer shows accurate user stats (cross-checked SQL).

 Zapier receives webhook within 2 s of event.

 Team queue loads with least-privilege access (cannot PATCH).

 No regression in existing Admin, Subscription, Teams tabs.

Branch: feature/admin-jv-enhancements
Deadline: deliver PR + Cypress tests; run full regression before merge.