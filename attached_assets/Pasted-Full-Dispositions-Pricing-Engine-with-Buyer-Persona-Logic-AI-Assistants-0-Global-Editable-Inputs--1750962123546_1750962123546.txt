Full Dispositions Pricing Engine with Buyer-Persona Logic & AI Assistants
0. Global Editable Inputs (top of Dispo panel)
Field	Default / Source	Notes
Acquisition Price $	Pulled from Acq tab, editable + comma-format	
ARV $	From comps, editable	
Repairs $	From header, editable	
Rent $/mo	From header, editable	
PITI $/mo	From Subject-To calc (if exists), editable	
Closing Costs $	Editable (default 3 % of price)	

These values drive every card below.

1. Wholesale Exit — Buyer-Persona Matrix
Create a mini-accordion «Wholesale Buyers» with four cards and auto formulas:

Persona	Formula	Notes
🏡 1 % Rural Rental	Price = Rent / 0.01	Only show if Rent≥1000 & property rural flag
🔁 Volume Flipper	Price = ARV − Repairs − 30 000 (profit target editable)	
🛠 BRRR Investor	Price = (ARV × 0 .70) − Repairs	
💸 Max-Profit Flipper	Price = (ARV × 0 .60) − Repairs (or slider 55-65 %)	

Each card shows:

Suggested assignment price

Buyer profit or cash-on-cash

Timeline badge (“30–60 days”)

Add radio button: “Select Target Buyer” → fills master Wholesale price field.

2. Novation / Installment (MLS Listing)
ini
Copy
Edit
ListPrice = (ARV − Repairs) − MAX( 10 % of (ARV−Repairs), 20 000 )
Display:

Commission sliders for List % and Buyer-Agent % (default 2 %/2 %)

Net to us after commissions & $20 475 fee table (editable)

3. Subject-To “Cash-to-Close” Builder
Inputs (right side of card):

Cash to Seller $ (default 5k)

Assignment Fee $ (default 15k)

Arrears/Liens $ (editable)

Closing Costs $ (auto 3 % or override)

ini
Copy
Edit
Cash-to-Close = CashToSeller + AssignmentFee + Arrears + ClosingCosts
Under the price, display helper tags:

Equity in deal: ARV − LoanBalance

Monthly Cash-Flow = Rent − PITI

Red if < $200, Gold 200-499, Green ≥ 500

4. Seller-Finance Wrap Configurator
Editable Controls inside modal:

Down Payment $ (slider 5–25 % of ARV default 10 %)

Buyer Interest % (auto ladder:
Down ≥ 15 % → 6 % APR ⬇️; Down < 10 % → 9 % APR ⬆️; never drops below underlying PITI rate)

Wrap Term yrs (5/10/30)

Repair Severity (None/Cosmetic/Heavy) → adds discount or ↑ price

Price to End-Buyer

cpp
Copy
Edit
BasePrice = ARV + 10 %   // if Repairs = None/Cosmetic
Heavy repairs → BasePrice = ARV   // no premium
Monthly Payment

ini
Copy
Edit
BuyerPI = Amortize(LoanAmt, BuyerRate, Term)  OR manual override
CashFlow = BuyerPI − PITI (must stay ≥ $0)
Profit shown as:

sql
Copy
Edit
Front-End Profit = DownPayment − (AcquisitionPrice − Any credits)
Monthly CashFlow
Warn if BuyerPI < PITI → disable save.

5. Lease-Option (Sub-Buyer)
Sliders:

Equity Available $ (auto, editable)

Repairs Needed (None / Some / Heavy)

Desired Cash-Flow $ (auto calc)

Heuristic Pricing:

nginx
Copy
Edit
BaseC2C = 8 % of ARV
+ $5 000 if CashFlow ≥ $350
+ $5 000 more if Equity ≥ 20 k
− $5 000 if Heavy Repairs
Display Cash-to-Close target + timeline (30-45 days).

6. AI-Powered Assistant Buttons (GPT-4o)
Button	Output
📊 Suggest Best Buyer Persona	GPT-4o reviews Wholesale matrix, cashflow, repairs & returns a ranked list with rationale
📋 Draft Buyer-Facing Pitch	AI writes 3-sentence pitch to each persona w/ price & benefits
❓ Handle Buyer Objection	Modal lets dispo rep type objection → returns answers using Chris Voss / Steve Trang etc. (same engine as Acq, flavored for buyers)

7. Visual Tweaks
Cash card = green, MLS = blue, Wrap = purple, LeaseOpt = orange

Show small “Powered by AI” badge on suggestion buttons

Keep Acquisition-derived numbers in a faint gray info-strip titled “Reference Values from Acquisitions”

📂 File/Component Suggestions
swift
Copy
Edit
/src/dispositions/
  WholesaleBuyerMatrix.jsx
  WrapConfigurator.jsx
  SubjectToCashClose.jsx
  LeaseOptionCalc.jsx
  BuyerObjectionModal.jsx
  aiDispoHelpers.js   // houses GPT-4o prompt builders
✅ Implementation Checklist for Ghostwriter
Render global editable deal-input strip.

Build Wholesale matrix + persona formulas.

Update Novation list-price logic.

Add Subject-To cash-close calc & validations.

Build Seller-Finance wrap modal with DP ↔ Rate ladder.

Add Lease-Option heuristic card.

Wire AI buttons via aiDispoHelpers.js using GPT-4o.

Drop this prompt into the repo, then let’s iterate!